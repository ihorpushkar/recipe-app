{
  "name": "recipe-app-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Recipe App API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "drizzle-orm": "^0.29.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240314.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
